# AetherCore Zenith Recursive Transformation Matrix

| Component | Weakness / root cause | Severity | Implemented solution | Verification method | Residual |
|---|---|---:|---|---|---|
| Operation Engine | Consent-visible duplicate DB columns could diverge from hashed immutable plan material | High | Cross-check immutable duplicates after digest verification and render the view from verified material | SQLite corruption regression; recursive gate; Windows workspace tests | SHA-256 beside the DB value is not a secret MAC against privileged local admin |
| Event Bus | Historical owner replay state grew monotonically | Medium | Bounded inactive-owner LRU-style retention; active subscribers never evicted; reconnect uses reset/hydration | Two EventBus unit regressions; structural gate | Evicted old cursors intentionally lose replay continuity and hydrate |
| Update Coordinator | Historical owner snapshot/release cache grew monotonically | Medium | Oldest-quiescent owner eviction at 128; mutation-critical states protected | Two update regressions; structural gate | Cap can soften if every entry is mutation-critical |
| Request admission | Per-session request limit could be bypassed by reconnect churn | High | Process-global 64-worker atomic RAII admission | Saturation unit regression; Windows churn/soak | A stuck worker intentionally consumes a slot until it exits |
| MutationSupervisor integration | Global mutation lease lifetime was tied to watcher, not privileged worker | High | Lease identity matching; remove unleased start entry points; worker-owned RAII lease in driver/repair/cleanup/startup execution | Kernel identity test; leased production routes; structural worker ownership gate | Full Windows mutation lifecycle remains native qualification |
| Read work budget | Expensive-read lease lifetime was tied to watcher, not collector | Medium/High | Remove unbudgeted scan entry points; worker-owned `ReadBudgetLease` for driver/repair/cleanup/startup/diagnostics | Kernel identity test; leased production routes; structural gate | Native provider cancellation/latency still needs fault testing |
| Worker creation | Bare thread spawn could panic after state/consent had advanced | High | Named fallible `Builder::spawn`; typed rollback/failure cleanup | Structural gate + Windows fault injection | OS resource exhaustion remains an availability event, now fail-closed |
| Stream watchers | Watcher spawn failure could abort request after work already started | Medium | Central fallible watcher helper; current state remains recoverable via hydration | Structural gate | Progress may be missed until reconnect/hydration when watcher creation fails |
| IPC/desktop/repair support threads | Thread creation and repair pipe reader teardown were not uniformly explicit | Medium | Fallible named threads; cancellation/accounting cleanup; child kill/wait + reader joins | Structural gate; Windows native fault injection | Synchronous pipe writer remains a separate residual |
| IPC synchronous I/O teardown | Synchronous writer/reader pumps had bounded queues but no active cross-thread cancellation path on teardown/backpressure | High availability / Medium security | Least-rights thread handles + `CancelSynchronousIo`; reader/writer cross-cancel; server reader binding; client pump fences post-shutdown writes | Recursive cancellation gates; IPC saturation regressions; Windows slow-peer/service-stop qualification | Synchronous pre-syscall cancellation race remains empirical; adopt overlapped I/O only if native evidence requires it |
| Fluid press | Fixed transition on direct feedback; cancel path lacked explicit activation fence | Medium | Immediate state feedback, spring transform, cancel/lost-capture click fence | Targeted TS compile + deterministic motion tests | Keyboard activation intentionally remains semantic click/keyboard behavior |
| Coordinator API surface | Legacy public unleased start APIs could bypass supervisor/budget in future callers | High architecture | Remove public unleased mutation/read entry points; require concrete non-optional leases at private boundaries; migrate tests | Recursive API-surface + mandatory-lease checks; workspace compile/tests | None by API surface; unsafe bypass would require code changes that trip the gate |
| IPC endpoint identity | Fixed-name client needed a documented proof of the connected privileged endpoint before `ClientHello`; client-side server-PID query is not a documented `CreateFile` contract | High security | Require `READ_CONTROL`; verify connected pipe owner = the service-specific SID and registered service = running own-process before protocol exchange; debug uses fresh 128-bit rendezvous | Recursive ordering/owner/service gates; native owner/DACL/service probe | Native Windows execution still required; compromised LocalSystem is outside this local standard-user boundary |
| IPC namespace ownership | First-creator race and identity-retry behavior could allow namespace squatting ambiguity | High security / availability | `FILE_FLAG_FIRST_PIPE_INSTANCE` on service startup; only open failures retry; opened-but-untrusted endpoint is terminal; release ignores debug rendezvous | Recursive first-instance/retry-order gates; Windows pre-squat/restart test | Startup collision/availability behavior remains native qualification |
| IPC namespace continuity | FIRST_INSTANCE protected startup, but sequential listener creation left a possible zero-instance gap under immediate disconnect/session churn | High availability / security defense | Separate `PipeServerListener` from sessions; synchronously create successor listener before admission/handoff; reclaim with FIRST_INSTANCE after accept failure | Recursive ordering/reclaim gates; Windows rapid-disconnect/squatter race | Native scheduler/timing evidence still required |
| IPC client retry policy | All pipe-open errors entered the same bounded retry loop | Medium security / availability | Retry only `ERROR_FILE_NOT_FOUND` / `ERROR_PIPE_BUSY`; fail every other open error immediately; keep opened-but-untrusted endpoint terminal | Rust retry-policy regression + recursive source gate | Windows startup/service-busy timing remains native qualification |
| IPC DACL / server-instance creation | `GENERIC_WRITE` for Authenticated Users also carries `FILE_CREATE_PIPE_INSTANCE` bit | High security | Exact AU mask `0x00120003` (`READ_DATA|WRITE_DATA|READ_CONTROL|SYNCHRONIZE`); exact client `access_mode`; bit `0x4` absent | Rust create-instance regression; recursive gate; native kernel DACL probe | DACL evidence must be captured from installed service |
| IPC client SQOS | Default named-pipe impersonation can exceed what authorization needs | High security | Explicit `SECURITY_IDENTIFICATION` on every production client open and native probe | Rust SQOS regression; recursive gate; Windows native probe | Native verification still required |
| Native IPC evidence policy | Probe accepted required-rights supersets and did not reject deny/inheritance/unexpected-trustee drift | Medium/High governance | Require exact AU allow mask, zero AU deny, protected DACL, exactly two allow ACEs (service + AU), no deny ACEs, no callback/object/opaque/flagged ACEs, expected trustees only, and generic-right-aware create-instance detection | Recursive probe-policy checks + native JSON evidence | Native script still must execute on qualified Windows |
| Machine mutation interprocess fence | Predictable `Global\...` mutex could be pre-created; existing-object semantics did not prove acquisition | High security / availability | Installer-provisioned protected ProgramData lock file + fail-immediate exclusive `LockFileEx`; runtime `OPEN_EXISTING` only; reparse-resistant open | Recursive centralization gate; installer ACL + contention verification | Native Windows contention/lifecycle evidence still required |
| Service-specific named-pipe server authority | Broad SYSTEM/Admin server ACEs were not least privilege and did not express the stable service identity | High security / availability | Dynamic service-SID SDDL: owner/service server authority = `NT SERVICE\AetherCoreMaintenance`; AU exact client mask only; bounded aligned SID lookup with post-length and `IsValidSid` validation, cached only after success | Descriptor regression; recursive service-SID gates; native owner/DACL probe | Must execute against installed service on Windows |
| Maintenance service token compatibility | Write-restricted service token conflicts with approved mutations against heterogeneous Windows/third-party ACLs | High functional/security architecture | `SERVICE_SID_TYPE_UNRESTRICTED`; service SID retained for AetherCore-owned ACLs and pipe identity; live token must have enabled-by-default/owner-capable service SID and zero restricting SIDs | Mutation-surface structural gate; installer policy checks; native `verify-maintenance-service-token.ps1` evidence | Must execute mutation compatibility matrix under installed service |
| Recursive validation | Token presence could hide malformed Rust declaration | Medium governance | Add syntactic-corruption meta-regression and exact diagnostic signature cardinality check | Current recursive gate result/count is generated only by `scripts/omega-evidence.py` | Rust compiler remains authoritative and must run on Windows CI |
| CI / release | New invariants were not part of historical release gates | High governance | Recursive audit wired into Zenith verification, CI, release preflight and production seal | Source-gate execution + workflow parsing | Cannot replace signing/native/hardware evidence |
