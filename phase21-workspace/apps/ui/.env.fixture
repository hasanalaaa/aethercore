# Development-only. Enables the sanctioned test transport so `layout-fixture.html`
# can replay a representative kernel stream and the layout sweep measures a
# POPULATED shell rather than an empty one. Never used by a release build:
# `vite build` runs in production mode and never reads this file.
VITE_AETHERCORE_TEST_TRANSPORT=1
