# Phase 35 Execution Plan

This plan is the execution contract for Distribution, Installation & Update Authority. Live source and executed evidence are authoritative; historical `PHASE_*_BINARY_SAFE_PATCH` trees are provenance only.

| ID / workstream | Objective and authoritative files | Architecture / exact tasks | Security invariants and tests | Acceptance / dependency / Hermes | Exact next step |
|---|---|---|---|---|---|
| P35-00 Baseline | Verify P34 archive, pointer, full-tree verifier, 930-check audit. Authoritative: `AetherCore-Phase34-Master-Delivery.zip`, `PHASE34_FINAL_SHA256.txt`, P34 patch/audit scripts. | Read-only SHA, full-tree and audit; external Graphify state check. | Any drift stops mutation. | PASS evidence recorded; prerequisite for all work. Hermes-safe: yes. | Create release-authority contract and lock decisions. |
| P35-01 Identity | One typed release identity. Authoritative: `Cargo.toml`, `apps/desktop/tauri.conf.json`, new `crates/release-authority`. | `AetherCore`, semver, channel, sequence, platform/arch, protocol/updater minimum, source identity; consistency verifier. | Fail closed on mismatch; no runtime Git. Unit and executable consistency tests. | Identity is the only version authority; all package/CLI surfaces derive or verify it. Hermes-safe: yes after D001. | Add crate and fixture identity. |
| P35-02 Manifest/signing/keyring | Canonical deterministic `aethercore.release.manifest.v1`; Ed25519 reuse; typed keyring and authorized rotation. | Struct-ordered JSON, digest excludes signature field, signature envelope, trusted key IDs/revocation/rotation signed by existing key. | Unknown/revoked/malformed/tampered keys reject; private keys test-only/external. Tests cover valid/tamper/rotation. | Manifest, digest, signature and keyring proofs. Hermes-safe: yes only within locked D002–D006. | Implement manifest/keyring APIs. |
| P35-03 Channels/update authority | Stable/beta/dev isolation, strict signed `aethercore.update.metadata.v1`, anti-downgrade and rollback authorization. | Typed channel policy, semver ordering, per-channel sequence floor, signed rollback artifact with target/current/expiry/reason. | No generic force bypass; wrong product/channel/platform/arch, stale replay, downgrade reject. | GD-3 passes. Hermes-safe: yes after D007–D009. | Add update metadata and rollback tests. |
| P35-04 Discovery/download/staging | Local/offline source and bounded HTTPS abstraction; verified staging. | Streaming hash/length, timeout/cancel, unique transaction staging, fsync then rename; no direct overwrite. | HTTPS-only production, no redirects/credentials/bypass; package eligible only after metadata/manifest/SBOM/provenance verification. | GD-4 and package attack tests. Hermes-safe: yes. | Add source/staging policy and hermetic fixtures. |
| P35-05 Archive safety | Explicit extraction policy. | Direct MSI/Burn is non-extracting; offline ZIP builder uses safe deterministic member validation. | Traversal, absolute/UNC/drive, symlink/hard-link, collisions, bombs and limits reject. | Gates prove safety or documented N/A for MSI extraction. Hermes-safe: yes. | Add Python offline bundle safety checks. |
| P35-06 Transaction/recovery/lock/plan | Durable typed update state machine and immutable apply plan. Authoritative: `crates/update-engine`, `crates/persistence`, new crate. | States Discovered→Validated→Downloading→Downloaded→Verified→Staged→ReadyToApply→Applying→Applied/RebootRequired or rollback/failure; journal + persistent single-update lock. | Illegal transitions, crash recovery, plan binding and substitution/installed-version revalidation fail closed. | GD-5 passes. Hermes-safe: yes after D010–D013. | Add typed state machine and proof tests. |
| P35-07 Windows installer | Canonical WiX/Tauri definition and semantics. Authoritative: `installer/wix/Product.wxs`, `Bundle.wxs`, `apps/desktop/tauri.conf.json`, `scripts`. | Stable UpgradeCode/component GUIDs, service registration, Program Files binaries, ProgramData state/log preservation, repair/upgrade/uninstall contracts. | No dev artifacts/secrets; ACL/Service SID/named-pipe policy explicit; runtime Windows claims remain P36. | GD-7 passes within macOS boundary. Hermes-safe: yes. | Add installer composition manifest and validation. |
| P35-08 Supply chain | Offline bundle, CycloneDX SBOM, project provenance and deterministic bindings. | SHA-256 binding from package→manifest→SBOM→provenance; no SLSA overclaim; normalized deterministic timestamps. | Unknown licenses stay unknown; no private credentials. | GD-6/GD-8 and deterministic ×2. Hermes-safe: yes. | Add generators and fixtures. |
| P35-09 CLI/desktop/i18n | Release/update CLI and safe desktop Update surface. Authoritative: `apps/aetherctl`, desktop main, system-care UI/catalogs. | JSON/text deterministic output, typed exit states, offline verify/select, UI calls typed backend only; EN/AR RTL parity. | No unverified apply/force; Mac apply returns NotAvailable. | GD-9 and CLI/UI tests. Hermes-safe: yes. | Extend CLI parser/usage and UI status vocabulary. |
| P35-10 CI/release | Harden `.github/workflows/release.yml` and release scripts. | Explicit tag/environment authority, test→SBOM→provenance→package→sign→verify→manifest→offline bundle; secrets only CI store. | No auto-publish, no secret logging, unsigned honesty. | CI static gates and GD-10. Hermes-safe: yes. | Add phase35 release workflow checks. |
| P35-11 Audit/docs/debt | P35 adversarial audit strict superset, append-only debt, full documentation. | Inherit P34 930 checks and add individually named P35 gates; write required docs/JSON/scorecard/progress. | Audit failures are real; qualification debt only for allowed Windows/live-server gaps. | >930 checks, zero failures. Hermes-safe: yes after architecture lock. | Implement audit and docs, checkpoint. |
| P35-12 Delivery/reconstruction/seal | Binary-safe P34→P35 patch, two reconstructions, deterministic archive twice, external hash pointer, post-seal read-only verification and Graphify refresh. | Delta manifest/ledger/apply/verify; archive excludes `.DS_Store`, self-reference artifacts only. | No post-seal tree writes; archive equality and reconstruction mismatch zero. | GA–GP all evidenced; final report exact required lines. Hermes-safe: preparation only; Codex reviews final security/seal. | Build patch, run gates, reconstruct ×2, seal and stop before P36. |

## Execution order

P35-00 → P35-01/02 → P35-03/04/05 → P35-06/07 → P35-08/09/10 → P35-11 → P35-12. After each meaningful workstream, update `PROGRESS.md`; locked decisions in `DECISIONS.md` are not reinterpreted by another executor.
