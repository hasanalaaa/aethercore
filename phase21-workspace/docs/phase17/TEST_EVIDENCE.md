# Phase 17 Test Evidence Policy

`reports/PHASE_17_STATIC_EVIDENCE.json` is generated by `scripts/phase17-intelligence-audit.py` and is the source-level evidence ledger for this delivery.

## Executed on this host

The Phase 17 audit executed **24 source/static checks and passed 24/24**. The existing platform-neutral Phase 0-16 `static_validate.py` gate was also rerun after integration and passed **342/342**; the same gate passes 342/342 on the unmodified Sigma baseline. It covers required source integration, typed fact/finding/remediation contracts, production driver-history normalization, deterministic rule inventory, explicit healthy semantics, immutable-plan validation, persistence behavior markers, localization parity and used-key coverage, Deep Scan UI motion/progress invariants, IPC route/event/hydration integration, SQLite v1-v10 migration execution in an in-memory database, sealed-plan foreign-key protection, qualification-debt structure, fixture inventory and prohibited placeholder/fake-progress patterns in Phase 17 files.

The SQLite migration smoke test is an executed database test: all ten migrations apply in order to an in-memory SQLite database, Phase 17 tables are present, and sealed remediation plans prevent deletion of referenced scans through `ON DELETE RESTRICT`.

The seven required synthetic PC scenarios are present as machine-readable fixtures and are mapped to Rust tests. Their **Rust execution is not claimed** on this host because Cargo/Rust are absent.

## Explicitly not executed

- `P17-RUST-001`: Cargo/Rust compilation and Rust test execution — `NOT_EXECUTED` because the Rust toolchain is unavailable.
- `P17-UI-001`: installed `svelte-check`/Svelte runtime validation — `NOT_EXECUTED` because UI dependencies are not installed. An attempted `npm run check` cannot execute `svelte-check` on this host.
- `P17-WINDOWS-001`: Windows-native collector, IPC, cancellation, performance and hardware qualification — `NOT_EXECUTED` by program strategy and host capability.

A gate is `PASS` only when it executed on this host. Source inspection is never relabeled as native or runtime qualification. Outstanding native requirements remain machine-readable in `QUALIFICATION_DEBT.json`.
