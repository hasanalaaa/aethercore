# Zenith Recursive Verification — Historical Provenance Notice

Earlier manually written verification totals in this document became stale as executable gates evolved. They have been removed rather than updated by hand.

For the current tree, run `scripts/omega-evidence.py` and consume its machine-readable output. That evidence records the actual current gate counts, source-manifest verification, dependency-freeze state, unsafe-boundary classification, browser interaction result, named-pipe teardown qualification state, toolchain availability, and release blockers.

Windows-native, signed, reproducible, stress-qualified, release-candidate, and GA states are never inferred from this historical file.
