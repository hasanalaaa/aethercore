packages:
  - 'apps/ui'

# pnpm 11 supply-chain guardrails. The lockfile is committed and production/CI use --frozen-lockfile.
minimumReleaseAge: 1440
blockExoticSubdeps: true
strictDepBuilds: true
allowBuilds:
  esbuild: true

auditLevel: high
